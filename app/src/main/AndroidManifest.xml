<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moesiof.minhasolimpiadas_escola">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Minhas Olimpíadas"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Registre sua Escola"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".RegisterResultActivity"
            android:label="Dados Cadastrais"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".EditActivity"
            android:theme="@style/LightDialogCustom">
        </activity>
    </application>

</manifest>